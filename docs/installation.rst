============
Installation
============

At the command line::

    $ easy_install django-outbox-middleware

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-outbox-middleware
    $ pip install django-outbox-middleware
